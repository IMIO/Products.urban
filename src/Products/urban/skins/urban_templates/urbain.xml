## Script (Python) "createurbanevent"
##title=CreateUrbanEvent
##bind container=container
##bind context=context
##bind namespace=
##bind script=script
##bind subpath=traverse_subpath
##parameters=datedu='',dateau=''
##

container.REQUEST.RESPONSE.setHeader('content-type', 'text/plain')
from Products.CMFCore.utils import getToolByName
catalog = getToolByName(context, 'portal_catalog')
results = catalog.searchResults(path = {'query' : '/'},portal_type = 'BuildLicence',review_state='accepted')
for obj in results:
    print obg/getTitle
return printed
